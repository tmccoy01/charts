# Environment Configuration for FAA Chart System
# Copy this file to .env and customize as needed.
#
# DEPLOYMENT:
# 1. Copy this file to .env
# 2. Set WMS_BASE_URL to your server's IP/hostname (e.g., http://192.168.1.150:8080)
# 3. Create log/tmp directories: mkdir -p logs/mapserv tmp/mapserv
# 4. Run: docker compose up -d

# =============================================================================
# Server Configuration
# =============================================================================

# External URL where clients (Google Earth) will access the WMS service
# Set this to your VM's IP or hostname
# Examples:
#   WMS_BASE_URL=http://192.168.1.150:8080
#   WMS_BASE_URL=http://charts.local:8080
WMS_BASE_URL=http://localhost:8080

# =============================================================================
# Path Configuration (Container paths - usually don't need to change)
# =============================================================================

MAPSERV_BASE_PATH=/home/mapserv
CHARTS_BASE_DIR=/home/mapserv/charts
MAPFILES_DIR=/home/mapserv/mapfiles
BIN_DIR=/home/mapserv/bin
PERL_LIB_DIR=/home/mapserv/perl
WORK_DIR=/home/mapserv/charts/work
INDEX_DIR=/home/mapserv/charts/index
BREF_DIR=/home/mapserv/charts/bref
CACHE_DIR=/cache

# =============================================================================
# Chart Download Configuration
# =============================================================================

# Chart types to download (comma-separated): sec, tac, hel, enrl, enrh, enra
CHART_TYPES=sec

# Delete ZIP files after extraction (saves disk space)
DELETE_ZIPS=1

# Chart cycle policy: "latest" (default) or "all"
CHART_CYCLE_POLICY=latest

# Force specific chart cycle (YYYYMMDD format, leave empty for latest)
CHART_CYCLE=

# =============================================================================
# Performance Configuration
# =============================================================================

# Number of MapProxy worker processes
MAPPROXY_PROCESSES=4

# GDAL cache size in MB
GDAL_CACHEMAX=768

# =============================================================================
# Debug Configuration
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# MapServer debug level (0=off, 1-5=increasing verbosity)
MS_DEBUGLEVEL=0
